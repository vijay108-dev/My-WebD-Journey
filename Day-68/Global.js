import { createContext } from "react";

const GlobalContext =  createContext("Aashu");

export default GlobalContext;

//first of all you need to create context of global file and data you will put it will return you an object and it will store into globalcontext
